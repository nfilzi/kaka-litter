<h1>Editing Product</h1>

<%= simple_form_for @product do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :designation %>
    <%= f.input :unit_price %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, "Edit product" %>
  </div>
<% end %>